# 7612 Hillside Dr — карточка проекта

**Заведён 2026-09-24 (локальный рабочий день, PT).** Второй реальный проект
после 545 Avenida Primavera. Карточка — ИНСТРУКЦИЯ И РЕЕСТР ФАЙЛОВ, не
спецификация: всё, что ниже помечено «не известно», именно не известно, а не
забыто.

## Что это

- Тип: кухня **Cesar**, моделируется в SketchUp через cabinet engine.
- Режим работы — demand-driven (CLAUDE.md): модель строится по реальному
  объекту; дыра в движке, найденная на этой кухне, становится следующей
  задачей движка и проверяется на этой же кухне.
- Правило «catalog fact или one-off?» действует с первого дня: особенность
  этого дома ложится в атрибуты юнита, а не в registry.

## Исходные данные

- **Замеры с объекта уже внесены в модель** — стены по замерам нарисованы в
  .skp (со слов Андрея, 2026-09-24). Отдельного листа замеров / фото в
  репозитории и в Documents нет.
- Чертежей архитектора/дизайнера: не известно, есть ли.
- Приборы (модели): не известно. Спецификации тянутся по запросу под
  конкретную модель, пакетом не храним.
- Потолок, окна, двери, MEP-точки (вода, слив, газ, электрика): не
  зафиксированы в этой карточке — читать из модели пробой, когда понадобится.
- Пол под шкафами: чистый (европейский вариант) или черновой — **развилка не
  решена для этого объекта**, спросить до первой базы.

## Файлы — по образцу 545, плоско в `~/Documents`

| файл | статус на 2026-09-24 |
|---|---|
| `7612_Hillside_Dr.skp` | есть; 43 491 байт, SketchUp 2025 (`{25.0.659}`). Рабочий черновик с замерами. |
| `7612_Hillside_Dr~.skp` | автобэкап SketchUp, не трогать |
| `7612_Hillside_Dr_Kitchen_Preliminary_Model_v0.1.skp` | **РАБОЧИЙ ФАЙЛ с 2026-09-24.** Создан Андреем через File → Save As. 28 242 байта — меньше исходника (43 491). Причина не установлена; до сверки содержимого в SketchUp исходник `7612_Hillside_Dr.skp` не трогать. Размер файла — не утверждение о документе (handoff 2026-09-08 §0.2). |
| `…_Preliminary_Model_v0.1.layout` / `.pdf` | будет, когда появится лист |
| `…_Preliminary_Model_v0_1_order.csv` | будет, из экспорта заказа (после починки `Sketchup.active_model`) |

- Trimble Connect — только для публикации снимков, живой файл там не держим
  (решение от 2026-09-07 по 545).
- Файлы 545 и 7612 лежат рядом в одной папке — пробы выбирают модель
  по имени (`UCON::ProbeBridge.model`), активное окно не использовать.

## Ограничение движка, которое касается проекта сразу

`Sketchup.active_model` — 31 точка вызова (handoff 2026-09-08 §2.2). Пока это
не исправлено, **при двух открытых моделях (545 и 7612) кнопка Export order
небезопасна**: она может взять не ту кухню. Пробы — только через
`UCON::ProbeBridge.model` с явным именем `7612_Hillside`.

## Фабрика

- Цикл: наш дизайн → запрос Элде → Metron → её заказ и SKP → сверка →
  приводим модель к фабричной или осознанно держим своё.
- По этому проекту Элде **ничего не отправлено**. Правило то же, что по 545:
  ничего не уходит на фабрику до утверждённого бюджета и полученного design
  retainer.

## Не известно — спросить Андрея, когда станет нужно

1. Клиент / кто заказчик (прямой клиент или через дилера/девелопера).
2. Коллекция Cesar, фасады, отделка.
3. Приборы: модели и panel-ready или нет.
4. Высота потолка и есть ли top elements до потолка.
5. Чистый или черновой пол под шкафами.
6. Сроки: когда нужен preliminary лист.

## Журнал

- 2026-09-24 — проект заведён: карточка, запись в `claude/README.md`.
  Модель с замерами уже существует; переименование в целевое имя — за
  Андреем.
- 2026-09-24 — Save As в целевое имя сделан. Размер нового файла меньше
  исходника; содержимое не сверено.
